# Control TLS Test Fixtures

These DER certificates and PKCS#8 keys are public, non-production test
material. They form one private test CA and two leaves with the canonical
coordinator and worker URI SANs used by `control_tls` unit tests. No deployment
trusts this CA.
